import * as sampleTypes from './sampleTypes';
import * as userTypes from './userTypes';

const reduxTypes = {
  sample: sampleTypes,
  user: userTypes
};

export { reduxTypes as default, reduxTypes, sampleTypes, userTypes };
